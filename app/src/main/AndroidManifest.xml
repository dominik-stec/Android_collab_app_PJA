<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mylego" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLEGO" >
        <activity android:name=".MainActivity2" >
        </activity>
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".services.RestService" />

<!--        <provider-->
<!--            android:name=".content_providers.RestContentProvider"-->
<!--            android:authorities="com.example.mylego.content_providers.RestContentProvider" />-->

<!--        <provider-->
<!--            android:name="com.example.mylego.content_providers.MyContentProvider"-->
<!--            android:authorities="com.demo.user.provider"-->
<!--            android:enabled="true"-->
<!--            android:exported="true"></provider>-->
    </application>

</manifest>